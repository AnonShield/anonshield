// PT -> EN para o conteudo dos slides (deck.ts). Fallback para PT quando ausente.
// Gerado/preenchido pela traducao; strings de codigo/numeros ficam de fora (fallback PT).
export const EN: Record<string, string> = {};
